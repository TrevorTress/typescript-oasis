import { useState, useEffect } from 'react';
import styled from 'styled-components';

const HomeComp = () => {
	return (
		<>
			<h1>Home</h1>
		</>
	);
};

const Home = styled(HomeComp)``;

export default Home;
